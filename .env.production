# Production build (Hugging Face Space): empty = same-origin.
# Flask serves the compiled frontend and the API from one URL, so the
# browser calls /api/... on its own origin. Leave this blank.
VITE_API_URL=
